# Dynamic Battery SVG React Component

This project creates an SVG react component which is dynamically changed with states.

## Start Project

After cloning, go to the root directory of the project and then

```bash
yarn
yarn start
```

